/**
 * Hook for managing workspaces with CRUD operations.
 *
 * Delegates to WorkspaceContext for shared state across all components.
 */

export { useWorkspaceContext as useWorkspace } from "../context/WorkspaceContext";
